if [ -t 0 ] && [[ -z $TMUX ]] && [[ $- = *i* ]]; then
  exec tmux
fi

alias vi=nvim
alias ll='ls -laF'
alias p='python3'

case ${OSTYPE} in
  linux*)
    alias open='xdg-open'
    alias pbcopy='xclip -selection c'
    alias pbpaste='xclip -selection c -o'
    ;;
  darwin*)
    ;;
esac
